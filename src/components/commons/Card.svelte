<div class="card">
    <slot></slot>
</div>

<style>
    .card {
        min-height: 20px;
        background-color: var(--bg-subdue);
        color: var(--color-subdue);
        margin-bottom: 1px;
        padding: var(--sm-padding);
    }
</style>